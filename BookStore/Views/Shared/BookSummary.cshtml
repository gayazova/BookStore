@model Book

<div class="well">
    <h3>
        <strong>@Model.Title</strong>
        <span class="pull-right label label-primary">
            @Model.Price
        </span>
    </h3>
    <span class="lead">@Model.PublishingHouse</span>
</div>

